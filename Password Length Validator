def check_password_length():
    while True:
        password = input("Please enter a password (8-15 characters long): ")
        if 8 <= len(password) <= 15:
            print("Password is within the acceptable range.")
            break
        else:
            print("Password must be between 8 and 15 characters. Please try again.")

check_password_length()
